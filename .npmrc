# esbuild <=0.24.2 vulnerability (GHSA-67mh-4wv8-2f99) affects the Vite dev
# server. This project runs no dev server - vitest is invoked as a test runner
# only. Upgrading vitest to v4 would require vite v6 (breaking). Accepted.
audit-level=high